# Ticketmaster Discovery API v2 — free key at https://developer.ticketmaster.com
# Without it the ticketmaster collector is skipped (with a warning), everything else runs.
TICKETMASTER_API_KEY=

# Canonical site URL used for canonicals, sitemap, JSON-LD (house standard: www)
SITE_URL=https://www.mobilebayevents.com
